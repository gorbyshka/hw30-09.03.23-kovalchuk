import React from "react";

const Send = (props) => {
    return (
        <div className="sends">
            <small className="send-name">{props.title}</small>
            
            <span id="car" class="material-symbols-outlined">airport_shuttle</span>
            
        </div>
    )
}

export default Send